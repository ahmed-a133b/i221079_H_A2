# SecureChat Configuration for Kali Linux Client
# Database runs on Windows Docker  
DB_HOST=192.168.88.1
DB_PORT=3305
DB_USER=scuser
DB_PASSWORD=scpass
DB_NAME=securechat

# Client connects to server (replace with actual Kali VM IP if needed)
# If server runs on same Kali VM, use localhost
SERVER_HOST=localhost
SERVER_PORT=8443

# Certificate Paths
CA_CERT_PATH=certs/ca-cert.pem
SERVER_CERT_PATH=certs/server-cert.pem
SERVER_KEY_PATH=certs/server-key.pem
CLIENT_CERT_PATH=certs/client-cert.pem
CLIENT_KEY_PATH=certs/client-key.pem

# Transcript Directory
TRANSCRIPT_DIR=transcripts